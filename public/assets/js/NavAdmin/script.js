const burgerAdmin = document.querySelector(".burger-admin");
const navbarAdmin = document.querySelector(".navlat");

burgerAdmin.addEventListener("click", () => {
	navbarAdmin.classList.toggle("active");
});
